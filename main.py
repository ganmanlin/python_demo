import pytest

# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    # pytest.main()
    # pytest.main(['test_mark', '-vs'])
    pytest.main(['test_mark', '-vs','-m','minus'])
